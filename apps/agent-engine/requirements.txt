fastapi==0.141.1
uvicorn[standard]==0.34.0
langgraph==0.2.62
langchain-anthropic==0.3.3
# 2.10.4 edi -> HAL QILIB BO'LMAYDIGAN konflikt: `google-genai` 2.11.0
# `pydantic>=2.12.5` talab qiladi, ya'ni `pip install -r requirements.txt`
# `ResolutionImpossible` bilan yiqilardi va `agent-engine` + `pip-audit`
# ishlarini BUTUNLAY bloklardi. 2.13.4 — hamma cheklovni qanoatlantiradigan
# eng kichik amaliy yechim (fastapi <3, langchain-anthropic >=2.7.4,
# anthropic >=1.9, pydantic-settings >=2.7.0), major KO'TARILMAYDI (2.x).
pydantic==2.13.4
pydantic-settings==2.7.1
# Lokal .venv bilan BIR XIL versiya — ilgari 0.43.0 pin qilingan edi, ya'ni
# lokalda test qilingan SDK-xulqi prod Docker'dagidan farq qilardi.
# langchain-anthropic 0.3.3 talabi (anthropic>=0.41,<1) bilan mos.
anthropic==0.113.0
# LLM ikkinchi provayderi (llm_utils): ANTHROPIC_API_KEY bo'lmasa GEMINI_API_KEY
# ishlatiladi. Anthropic keyin qo'shilsa avtomat ustuvor bo'ladi.
google-genai==2.11.0

# SEC-15 — XAVFSIZLIK POLI (tranzitiv bog'liqlik).
# `cryptography` to'g'ridan-to'g'ri ishlatilmaydi: u google-genai -> google-auth
# zanjiri orqali keladi va requirements'da PIN QILINMAGAN edi, ya'ni qaysi
# versiya o'rnatilishi resolver kayfiyatiga bog'liq edi. 49.0.0 da
# PYSEC-2026-3552 bor (tuzatilgan: 50.0.0). Bu — POL (`>=`), qat'iy pin emas:
# google-auth kelajakda yangiroq versiyani talab qilsa to'sib qo'ymaydi,
# lekin zaif versiyaga TUSHIB QOLISHNI imkonsiz qiladi.
cryptography>=50.0.0
# Phase 5 (P5.1) — xato kuzatuvi. VERSIYA QOTIRILGAN (repo konvensiyasi):
# yuqori oqimdagi jimgina o'zgarish prod xulqini o'zgartirmasin.
# `[fastapi]` ekstrasi ATAYLAB YOZILMAGAN: u faqat `fastapi`+`starlette`
# ni tortadi, ular esa allaqachon yuqorida PIN QILINGAN — ekstra ularning
# pinini yumshatib, mos kelmagan versiyani o'rnatishi mumkin edi.
# Integratsiya klasslari bazaviy paketda mavjud (`sentry_sdk.integrations.fastapi`).
sentry-sdk==2.67.1
numpy==2.2.1
httpx==0.28.1
sse-starlette==2.1.3
# PYSEC-2026-2270 (CVE-2026-28684) — 1.0.1 zaif, tuzatilgan 1.2.2.
# TO'G'RIDAN-TO'G'RI bog'liqlik va MINOR ko'tarish (1.x ichida), hech bir
# cheklovga zid emas (`pydantic-settings` >=0.21.0, `uvicorn` >=0.13 —
# ikkalasi ham qanoatlanadi). ADR-022 dagi major'lardan FARQLI o'laroq
# buni kechiktirish uchun sabab YO'Q.
python-dotenv==1.2.2
# DIQQAT — pillow 11.1.0 da 24 ta advisory bor va uni HECH BIR cheklov
# ushlab turmaydi (`google-genai` uni versiyasiz talab qiladi), ya'ni
# 12.3.0 ga ko'tarish TEXNIK jihatdan mumkin. U ATAYLAB ko'tarilmadi:
# 11 -> 12 MAJOR va ADR-022 major'larni Phase 6 ga qoldirgan.
# Batafsil: ADR-022 "Long-term impact" + ci-red-2026-08-12.md §3.
pillow==11.1.0

# H4/M5: kamera (RTSP -> YOLO -> Claude Vision) og'ir CV-kutubxonalari
# (opencv/onvif/ultralytics/torch, ~1-2GB) BAZAVIY o'rnatishdan CHIQARILDI ->
# `requirements-camera.txt`. Sabab: render free-plan (512MB RAM) engine'da
# torch import bo'lsa OOM xavfi bor edi, holbuki kameradan deyarli hech kim
# foydalanmaydi. main.py endi camera_router'ni himoyalangan import qiladi (M5):
# CV-paketlar bo'lmasa faqat /camera/* o'chadi, engine to'liq ishlaydi.
# Haqiqiy kamera qo'shilganda:  pip install -r requirements-camera.txt
