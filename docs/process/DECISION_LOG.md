---
doc: DECISION_LOG
version: 1.0
status: ACTIVE
created: 2026-08-14
last_verified: 2026-08-14
supersedes: —
superseded_by: —
---

# DECISION LOG — append-only

**Qoida:** [`SPEC_SYSTEM.md`](SPEC_SYSTEM.md) §5.4.

> **Bu fayl APPEND-ONLY.** Qator **hech qachon o'chirilmaydi va
> tahrirlanmaydi** — faqat pastga qo'shiladi. Qaror bekor qilinsa, uni
> o'chirish emas, **yangi qator** bilan bekor qilish yoziladi
> ("X qarori bekor qilindi, sabab: …").

**Har qator qachon yoziladi:**

- Hujjat haqiqatga zid chiqdi va tuzatildi (§5.3 oltin qoida)
- `[CALIBRATE]` raqam `[MEASURED]` ga aylandi
- Blueprint yoki task spec o'zgardi
- Verification `FAIL` bo'ldi, lekin qarz **ochiq sabab bilan qabul
  qilindi** (§6.4 istisnosi)
- Roadmap ustuvorligi o'zgardi

---

| Sana | Nima o'zgardi | Nega | Ta'sirlangan hujjatlar |
|---|---|---|---|
| 2026-08-14 | V3 strategiya to'plami yaratildi (roadmap, build-vs-buy, pricing, metrics, business, safety, kill-criteria + ADR-023…032) | Contract §3 tartibi metering'ni Phase 7 ga qo'ygan edi; zarar hajmi o'lchanmagani aniqlandi (`Message.tokensIn` kodda 0 marta yoziladi) | `strategy/*`, `adr/ADR-023…032`, `ENGINEERING_CONTRACT_ADDENDUM_V3.md` |
| 2026-08-14 | ADR-001…020 Contract §5 dan alohida fayllarga ko'chirildi | Contract §5 buni talab qilgan, lekin bajarilmagan edi | `adr/ADR-001…020` |
| 2026-08-14 | Spec System qabul qilindi (to'rt qavat, JIT blueprint, uch tier task spec, mashinada tekshiriladigan DoD, yangi-sessiya verification) | Qaror egaligini ajratish: Claude Code arxitektor emas, ijrochi bo'lishi kerak | `process/SPEC_SYSTEM.md`, `process/DECISION_LOG.md` |
| 2026-08-16 | Free tier balans-yechishdan **kunlik hisoblagichga** o'tdi (10/kun, `chargeForMessage` free'da no-op) + LLM zanjiri **OpenRouter ko'p-model rotatsiyasiga** (5 ta `:free`, tool-calling'li) + hisob darajasidagi buferli budjet (45/kun, Redis) | Nol byudjet: har bepul obunachiga pullik chaqiruvni moliyalashtirib bo'lmaydi. Bundan tashqari diagnostika ko'rsatdiki, yangi foydalanuvchi balansi 0 bo'lgani uchun BIRINCHI xabarda 402 olardi — ya'ni free tier amalda mavjud emas edi | `strategy/PRICING_ARCHITECTURE.md` §3.0/§3.1, `strategy/BUILD_VS_BUY.md` (model routing qatori), `strategy/SAFETY_POLICY_LAYER.md` §8bis |
| 2026-08-17 | **Cron soni tuzatildi: 8 → 7.** Oldingi auditlar `grep "@Cron"` ning 8 moslik bergani asosida "8 ta cron" deb yozgan; sakkizinchi moslik — `apps/api/src/redis/cron-leader.service.ts:8` dagi **hujjat izohi**, dekorator emas. Qat'iy o'lchov `grep -rn '^\s*@Cron(' apps/api/src --include=*.ts \| grep -v spec` → **7**. Yon topilma: `runExclusive` 7 faylda uchraydi, lekin haqiqiy qamrov — **2/7 cron** (3 spec + qulf servisining o'zi + `marketplace.service.spec.ts` uni faqat mock qiladi, `marketplace.service.ts` chaqirmaydi) | SPEC_SYSTEM §5.3 (oltin qoida): hujjatdagi raqam haqiqatga zid chiqdi. Amaliy ta'siri — P0-4 (cron taqsimlangan qulf) qamrovi 6 emas, **5 cron**: `impersonation-admin:225`, `briefing:91`, `goals:150`, `alert-evaluator:63`, `competitor-price:131`. **Bajarilishi kerak:** `strategy/METRICS.md` §1 qatori tuzatiladi yoki `STALE` deb belgilanadi. `status/current-state-2026-08-13.md` §1 esa ATAYLAB tuzatilmaydi — u o'z header'ida muzlatilgan snapshot deb e'lon qilingan ("YANGILANMAYDI", §5.2 `ARCHIVED`); tuzatish shu jurnal va blueprint'da yashaydi | `strategy/METRICS.md` §1 (tuzatilishi kerak), `status/current-state-2026-08-13.md` §1 (muzlatilgan — tegilmaydi), `blueprints/P0_BLUEPRINT.md` §1.1/§1.2 |
| 2026-08-17 | **P0-3 (SEC-07 domain allowlist) implement qilindi — env darajasida.** Yangi: `automation/domain-allowlist.ts` (+ 2 spec), `BrowserBridge` ga allowlist opsiyalari, `AutomationService` da `DeviceActionLog` blok yozuvi, `.env.example` da `AGENT_DOMAIN_ALLOWLIST`/`_ENFORCE`. 80 test, 0 regressiya (151 test qo'shni suite'larda), migratsiyasiz | Contract SEC-07 — yagona yopilmagan Critical; blueprint §2.8 D2 (safe demo path) unga bog'langan. Env yo'li tanlandi, chunki u migratsiya talab qilmaydi va AC ni to'liq qoplaydi | `blueprints/P0_BLUEPRINT.md` P0-3 §16 (DoD holati belgilandi), `.env.example` |
| 2026-08-17 | **P0_BLUEPRINT P0-3 §6 dagi ma'lumot modeli NOTO'G'RI edi, tuzatildi.** Blueprint `Agent.toolsConfig` ni `{ browser: { allowedDomains } }` obyekt deb yozgan; haqiqatda u **massiv** (`schema.prisma:490`: "JSON array of ToolSpec", `CreateAgentDto.toolsConfig?: ToolSpecDto[]`). Bundan tashqari `AutomationService.run()` da `agentId` **umuman yo'q** — brauzer oqimi agentga bog'lanmagan | SPEC_SYSTEM §5.3: hujjat haqiqatga zid chiqdi, o'sha sessiyada tuzatildi. Oqibati: agent-darajasidagi allowlist va "6 domen → 400" DoD bandi **bajarilmadi** va ochiq qarz sifatida yozildi; `resolveAllowlist({env, agent})` interfeysi tayyor turibdi | `blueprints/P0_BLUEPRINT.md` P0-3 §6 (tuzatish bloki), §7, §16 |
| 2026-08-17 | **P0-3 rollback semantikasi aniqlashtirildi:** `AGENT_DOMAIN_ALLOWLIST_ENFORCE=false` "shadow rejim" (log yozib, bloklamaslik) EMAS — u tekshiruvni **umuman bajarmaydi** | Bo'sh allowlist (`domains: []` = "hech narsaga ruxsat yo'q", fail-closed) va majburlashning o'chirilishi ("tekshirma") — ikki xil holat. Bitta bayroqqa siqilsa, fail-closed jimgina fail-open'ga aylanardi. Kodda alohida `enforceDomainAllowlist` bayrog'i bilan ajratildi | `blueprints/P0_BLUEPRINT.md` P0-3 §15, `automation/browser-bridge.ts` |
| 2026-08-17 | **UI verifikatsiyasida SKRINSHOT MAJBURIYATI BEKOR QILINDI.** `P0_BLUEPRINT` §6.2 dagi "DEMO MODE / ENGINEERING MODE" ikki rejimi va har holat uchun skrinshot talabi olib tashlandi; o'rniga **dasturiy tekshiruv V1–V4** (statik → sahifa ko'tarilishi → konsol tozaligi → DOM va `getComputedStyle` assertlari: holatlar, token qiymatlari, kontrast ≥4.5:1, overflow, truncate, klaviatura, reduced-motion). "Founder tasdiqlaguncha keyingi task boshlanmaydi" qoidasi ham bekor — tasdiq endi bloklovchi emas | Founder qarori (2026-08-17). Texnik jihatdan bu **kuchliroq** dalil: skrinshot "shunday ko'rinadi" deydi, DOM+uslub asserti "aynan shu token, aynan shu qiymat" deydi va takrorlanadi. Bloklovchi tasdiq esa solo founder uchun ijro to'sig'iga aylanardi (SPEC_SYSTEM §10) | `blueprints/P0_BLUEPRINT.md` §6.2, §9 (9-savol), UI-1…UI-11 DoD bandlari |
| 2026-08-17 | **UI-1 (dizayn tili asosi) bajarildi.** Yangi semantik tokenlar: 4 risk tier + 6 ijro holati; yangi `ui/status.tsx` (RiskBadge/StateBadge/StatusDot) va `ui/data-row.tsx` (DataList/DataRow); `Button` ga `loading`, `Input`/`Textarea` ga `error` holati; `/design-system` ichki sahifa (dev-only, 11 bo'lim). Imzo qarori: **risk tier rang bilan emas, "tiriklik darajasi" bilan** ko'rsatiladi (LOW tekis → CRITICAL 60bpm puls) | Liquid Obsidian qonuni #3 ("raqam emas — holat") va #4 ("hech narsa qotib turmaydi") tizimning o'z tili; yangi vizual lug'at import qilish uni buzardi. A11y: rang yakka o'zi signal emas — har tier o'z shakli bilan ham ajraladi, va reduced-motion'da CRITICAL porlashi HIGH dan kuchliroq (harakatsiz ham farq ko'rinadi) | `apps/web/src/app/globals.css`, `components/ui/{status,data-row,button,input}.tsx`, `app/design-system/*`, `src/middleware.ts` (dev-only yo'l), `blueprints/P0_BLUEPRINT.md` UI-1 §10 |
| 2026-08-17 | **P0-13 (Event Bus) + P0-7 (Trace) BIRGA bajarildi.** Migratsiya `20260817120000_execution_trace_events`: 3 enum (`RunStatus`, `ExecutionEventType` 16 qiymat, `EventActor`) + 2 model (`ExecutionRun`, `ExecutionEvent`, `@@unique([runId, seq])`). Yangi `src/events/`: `ExecutionEventBus` (yagona yozuv nuqtasi, MAJBURIY redaksiya, run-ichi monotonik `seq`, P2002 retry, bo'ron chegarasi, SSE obunasi), `ExecutionRunService` (kursorli ro'yxat, egalik, `?after=` teshik to'ldirish), `POST /internal/execution-events` (InternalTokenGuard), `GET /runs[/:id][/events][/stream]`. 28 test + jonli tekshiruv (401/401/400/fail-open) + uchdan-uchgacha (3 hodisa, sir bazaga tushmadi, `tokensIn` saqlandi) | Blueprint §5: ular "bitta juftlik, rollback birligi bitta" — P0-13 ning o'z DoD'i jadvalni talab qiladi, ya'ni doimiy qatlamsiz yopib bo'lmaydi. Redaksiya `emit()` ICHIDA: iste'molchida bo'lsa kimdir "men allaqachon tozaladim" deb o'tkazib yuborardi | `apps/api/prisma/schema.prisma`, `prisma/migrations/20260817120000_*`, `src/events/*`, `src/app.module.ts`, `blueprints/P0_BLUEPRINT.md` P0-13 §16 |
| 2026-08-17 | **Migratsiya repo qarzi aylanib o'tildi, YOPILMADI.** `prisma migrate dev` butun dev bazasini reset qilishni talab qildi — sabab: `20260809220000_sec12_impersonation_…` migratsiyasi qo'llanilgandan KEYIN tahrirlangan (checksum mos emas). Reset qilinmadi; o'rniga `migrate diff` → fantom `DROP TABLE "AuditLogHashBackup"` olib tashlandi → `db execute` → `migrate resolve --applied` | Reset = dev bazadagi barcha ma'lumot yo'qolishi. Aylanma yo'l bir xil natija beradi (`migrate status` sinxron), lekin **asosiy qarz ochiq**: keyingi `migrate dev` yana shu devorga uriladi. Yopish alohida task — tahrirlangan migratsiya checksumini tiklash yoki `AuditLogHashBackup` ni sxema bilan moslashtirish | `blueprints/P0_BLUEPRINT.md` P0-13 §16 (migratsiya eslatmasi), `prisma/migrations/*` |
| 2026-08-17 | **P0-13 da ijro yo'llari busga ULANMADI** — chat streaming va `AutomationService` hali `RUN_STARTED`/`TOOL_*` yubormaydi; jadval bugun faqat `/internal/execution-events` orqali to'ladi | Ulash chat oqimining pul va halal-filter yo'llariga tegadi va uni **UI-4 bilan birga** qilish to'g'ri: aks holda hodisalar yoziladi, lekin ularni hech kim ko'rmaydi — o'lik yozuv (Konstitutsiya #38 ruhi). Keyingi task: UI-4 + ulash | `blueprints/P0_BLUEPRINT.md` P0-13 §16 (qamrov eslatmasi) |
| 2026-08-17 | **UI-4 QISMAN bajarildi + ijro yo'li busga ULANDI.** Yangi `execution-trace-tap.ts` — engine SSE lug'atini kanonik §2.3 ro'yxatiga tarjima qiladi (`tool_result`+`calling`→`TOOL_STARTED`, halal hodisalari→**`POLICY_CHECK`**, birinchi `token`→`MODEL_STARTED`); `openChatStream` run yaratadi va `runId` ni oqimning birinchi hodisasi sifatida qaytaradi; BFF `agentId` ni uzatadi (ilgari tashlab ketardi); web: `StepCard`, `useRunEvents` (SSE + `?after=` teshik to'ldirish), trace sahifasi `/agents/[id]/runs/[runId]`. 93 test, i18n 31 kalit × 3 til (922=922=922), `next build` exit 0 | Blueprint P0-13 da yozilgan edi: bus qurilgan, lekin ulanmagan — "hodisalar yoziladi, lekin ularni hech kim ko'rmaydi" holatini yopish uchun ulash UI-4 bilan BIRGA qilindi. Tarjima yagona nuqtada (backend): frontendda takrorlansa ikkita haqiqat manbai bo'lardi | `apps/api/src/events/execution-trace-tap.ts`, `agents.service.ts`, `agents.controller.ts`, `dto/chat-stream.dto.ts`, `web/src/components/chat/step-card.tsx`, `lib/use-run-events.ts`, `app/(dashboard)/agents/[agentId]/runs/[runId]/page.tsx`, `blueprints/P0_BLUEPRINT.md` UI-4 §10 |
| 2026-08-17 | **Trace tap testi haqiqiy bug topdi va tuzatildi:** `finish()` stream hodisa handlerlaridan (`end`/`error`/`close`) chaqiriladi va u yerdan qochib ketgan `emit` xatosi Node'da `uncaughtException` ga aylanib **butun API jarayonini yiqitardi** — ya'ni kuzatuv qatlami xatosi xizmatni o'ldirardi. `emit` va `finish` ichiga `try/catch` qo'yildi (chaqiruv nuqtalariga tarqatilmadi) | Trace fail-open bo'lishi kerak degan qoida faqat `bus.emit` darajasida ta'minlangan edi; sinxron throw (mock yoki kelajakdagi bug) uni chetlab o'tardi. Test yozilmasa bu prod'da topilardi | `apps/api/src/events/execution-trace-tap.ts`, `execution-trace-tap.spec.ts` |
| 2026-08-17 | **P0-6 (policy engine + kill switch + approval logging) bajarildi — G0.3, G0.4, G0.5 YOPILDI.** Migratsiya `20260817140000_policy_and_kill_switch`: `RiskTier`/`ApprovalDecision` enumlari, `Agent.killedAt/killedById/killReason`, `ApprovalEvent` modeli. Yangi `src/policy/`: `PolicyEngine` (8 o'lchovli qaror, faqat tier KO'TARADI, fail-closed), `KillSwitchService` (agent + global, dual confirmation), `ApprovalService`. `ConnectorDefinition.limits` **majburiy maydon** qilindi (TypeScript majburlaydi) va 17/17 konnektorga SAFETY §3.2 bo'yicha to'ldirildi. Policy darvozasi `ConnectorsService.invoke` ichida — yagona ijro nuqtasi. 44 policy testi, jami 147 test / 13 suite, 0 regressiya | Contract SEC-07 dan keyin qolgan asosiy xavfsizlik bo'shlig'i; §2.8 D4 (demo'da STOP namoyishi) va UI-4 ning qolgan qismi shunga bog'liq. `limits` ni `?` bilan ixtiyoriy qilish 17/17 holatini yana "keyin qo'shamiz" ga aylantirardi — shuning uchun majburiy | `apps/api/prisma/schema.prisma`, `prisma/migrations/20260817140000_*`, `src/policy/*`, `src/connectors/{connector.types,connectors.service,connectors.module}.ts`, `src/connectors/connectors/*.ts` (17), `scripts/check-connector-limits.mjs`, `blueprints/P0_BLUEPRINT.md` P0-6 §16 |
| 2026-08-17 | **P0-6 da "tasdiqlab davom etish" QILINMADI — bu ochiq cheklov.** Policy `HIGH`+ amalni bloklaydi va `ApprovalEvent` yozadi, lekin foydalanuvchi tasdiqlagach amal avtomatik davom etmaydi — qayta boshlanishi kerak | Ijroni to'xtatib **aynan shu joydan** tiklash — **P0-8 (LangGraph checkpoint)** ishi, u hali bajarilmagan. G0.5 gate'i ("0 ta chetlab o'tish") bundan ta'sirlanmaydi: xavfli amal tasdiqsiz hech qachon bajarilmaydi. Yetishmayotgani — qulaylik, xavfsizlik emas | `blueprints/P0_BLUEPRINT.md` P0-6 §16 (P0 cheklovi bloki) |
| 2026-08-17 | **Blueprint DoD misoli (`gmail.read` vs `gmail.send`) haqiqiy konnektorlarga ko'chirildi** — `gmail` 17 konnektor registrida YO'Q. O'rniga: `google-sheets.read_range` → LOW vs `telegram-bot.send_message` (10 tashqi qabul qiluvchi) → CRITICAL | Blueprint misoli illyustrativ edi (SAFETY_POLICY_LAYER §3.2 jadvalidan emas). Tamoyil o'zgarmadi: tool NOMIGA qarab tier belgilash o'qish va yuborish farqini ko'rmaydi — alohida test ikkalasi bir xil emasligini qulflaydi | `blueprints/P0_BLUEPRINT.md` P0-6 §16, `src/policy/policy-engine.spec.ts` |
| 2026-08-17 | **⚠️ `ApprovalService.request()` NI HECH KIM CHAQIRMASDI — tuzatildi.** P0-6 da servis yozilgan va `APPROVAL_REQUIRED` hodisasini yuboradi, lekin chaqiruv nuqtasi yo'q edi: policy darvozasi amalni bloklardi va auditga yozardi, biroq `ApprovalEvent` yaratmasdi. Natijada approval kartasi chatda **hech qachon chiqmasdi** va HITL qatlami amalda mavjud bo'lmasdi. `policyGate` endi `requiresApproval` bo'lganda so'rov yozadi | UI-4 (b) ni ulashda aniqlandi. Bu — "kod yozilgan, lekin oqimga ulanmagan" sinfidagi xato: testlar servisni alohida tekshirardi va u o'tardi | `apps/api/src/connectors/connectors.service.ts`, `blueprints/P0_BLUEPRINT.md` UI-4 §10 (tuzatish bloki) |
| 2026-08-17 | **`ApprovalEvent.runId` uchun vaqtinchalik ko'prik:** engine `/connectors/internal/invoke` da `runId` uzatmaydi, shuning uchun `resolveActiveRunId()` qo'shildi — u **faqat AYNAN BITTA** `RUNNING` ijro bo'lganda uni tanlaydi; nol yoki bir nechta bo'lsa `null` | Taxminiy run tanlash tasdiq so'rovini BOSHQA ijroga bog'lab qo'yardi va foydalanuvchi noto'g'ri kontekstda "ha" bosardi — bu xavfsizlik yo'lida qabul qilinmas. To'g'ri yechim: engine `runId` ni uzatishi (Python tomonidagi o'zgarish, alohida task). Ikkala holatda ham amal BLOKLANADI — G0.5 ta'sirlanmaydi. DB nosozligida ham 500 emas, `null` (bloklangan amal tushunarli javob berishi kerak) | `apps/api/src/connectors/connectors.service.ts`, `blueprints/P0_BLUEPRINT.md` UI-4 §10 |
| 2026-08-17 | **PUL QARORI (founder): birinchi agentning YARATISH narxi kechiriladi.** Yangi foydalanuvchining balansi 0; generik narxlash yaratishni bepul qiladi (`priceForAgent` → `creationUsd: 0`), lekin shablon o'rnatish katalog narxini aniq uzatadi (`createUsd: 70`) → `agents.service.ts` balans talab qilib **402** qaytarardi. Ya'ni onboarding'ning shablon yo'li birinchi qadamdayoq o'lardi. Endi `isFirstAgent` pul yechishdan OLDIN hisoblanadi va birinchi agentda `chargeableCreationTiyin = 0` | Mavjud niyatning davomi: platforma birinchi agentni allaqachon **sinov** deb belgilaydi (`isTrialAgent`, 3 kun / 20 xabar) — oylik to'lovni kechirib, yaratish uchun $70 talab qilish o'sha niyatga zid edi. Daromad YO'QOLMAYDI, kechikadi. ⚠️ Men bu o'zgarishni avval bir tomonlama qilib, keyin qaytardim — ikkita mavjud test uni ataylab qulflagan edi va bu pul qarori founder'niki | `apps/api/src/agents/agents.service.ts`, `agents.service.spec.ts` (2 test yangilandi + 2 yangi), `blueprints/P0_BLUEPRINT.md` UI-2 §10 |
| 2026-08-17 | **Kechirilgan yaratish `CreditLedger` ga YOZILMAYDI**, faqat `AuditLog` ga (`chargedCreationTiyin`, `waivedReason: 'first_agent_trial'`) va `Agent.creationPriceTiyin = 0` ga | `CreditLedger` — pul O'ZGARISHLARI jurnali (Konstitutsiya #17). 0 summali no-op qator uni ifloslantirardi; katalog narxini (`−70`) yozish esa balans o'zgarmagan holda jurnalni YOLG'ON qilardi (#18 bilan zid) | `apps/api/src/agents/agents.service.ts` |
| 2026-08-17 | **UI-4 trace ulanishi tasodifan o'chirilib, tiklandi.** Pul o'zgarishini qaytarishda `git checkout -- agents.service.ts` ishlatdim va u bilan birga `openChatStream` dagi trace tap ham ketdi. Tiklashda yaxshilandi: `runId` manbai endi `dto.agentId` (BFF uzatadi) va **egalik tekshiriladi** | `agentDefinition` — mijoz quradigan obyekt; undagi `agent_id` ga ishonib iz yozilsa, foydalanuvchi BEGONA agent nomiga hodisa yozdira olardi (IDOR). To'liq to'plam qayta yashil: 83 suite / 1163 test | `apps/api/src/agents/agents.service.ts` |
| 2026-08-17 | **P0-8 saqlash joyi qarori O'ZGARDI: engine Postgres'ga ULANMAYDI, holat API orqali saqlanadi.** Blueprint §5 `langgraph-checkpoint-postgres` → mavjud Postgres degan edi; implementatsiyada aniqlandiki `apps/agent-engine` da DB kutubxonasi UMUMAN yo'q (`requirements.txt` da `psycopg`/`sqlalchemy`/`asyncpg` yo'q) va `render.yaml` engine'ga `DATABASE_URL` bermaydi. Yangi yechim: `AgentCheckpoint`/`AgentCheckpointWrite` jadvallari API'da, engine tomonda `ApiCheckpointSaver` (`BaseCheckpointSaver`) `/api/internal/checkpoints` ga boradi | Blueprint qarori amalda "engine'ga DB kirishini berish" degani edi — arxitektura chegarasini ochish (engine buzilsa hujumchi to'g'ridan-to'g'ri bazada) + 2 yangi bog'liqlik (Konstitutsiya #40 → ADR). Founder ko'rsatmasi: "eng kerakli joyda saqla" → holat Postgres'da (A10), lekin unga yozish API orqali. Natija: **yangi Python bog'liqligi 0**, engine DB'ga tegmaydi, holat restart'dan omon qoladi | `apps/api/prisma/schema.prisma`, `prisma/migrations/20260817160000_agent_checkpoints`, `src/events/checkpoint.{service,controller}.ts`, `agent-engine/api_checkpointer.py`, `agent_engine.py`, `.env.example` |
| 2026-08-17 | **P0-8 da `checkpointer` argumenti uchun sentinel (`_UNSET`) ishlatildi** — `None` "berilmagan" bilan aralashmasin | Uch holat kerak: berilmagan → muhitdan tanlanadi (prod); `None` → ATAYLAB o'chirilgan (graf mantig'ini tekshiruvchi 4 test); obyekt → aynan shu. `None` ni default qilish ikkinchi holatni imkonsiz qilardi va mavjud testlar checkpointer bilan ishga tushib yiqilardi | `apps/agent-engine/agent_engine.py`, `test_engine.py` |
| 2026-08-17 | **P0-8 oqimga ULANMADI — ochiq qarz.** `resume(run_id)` metodi yozildi, lekin (a) uni chaqiradigan `POST /agents/resume` endpointi yo'q, (b) chat oqimi (`/agents/stream`) LangGraph grafidan umuman o'tmaydi (alohida `streaming.py` yo'li), ya'ni chat uchun checkpoint hozircha qo'llanmaydi | Infratuzilma (saqlash, LangGraph semantikasi, fail-open, rollback) tayyor va testlar bilan qoplangan; oqimga ulash alohida task. Blueprint DoD'idagi "3 qadam → resume → 4 chaqiruv" testi shu ikki bo'g'insiz sun'iy bo'lardi, shuning uchun yozilmadi va ochiq belgilandi. Bu — P0-6 dagi "tasdiqlab davom etish yo'q" cheklovining ayni ildizi | `blueprints/P0_BLUEPRINT.md` P0-8 §16 (qolgan qarz bloki) |
| 2026-08-17 | **"Tasdiqlab davom etish" YOPILDI — va streaming yo'liga TEGILMADI.** `ApprovalService.decide()` endi `APPROVED`/`MODIFIED` da `ConnectorsService.invokeApproved()` ni chaqiradi va trace zanjirini yozadi (`APPROVAL_GRANTED` → `TOOL_STARTED` → `TOOL_RESULT`/`TOOL_FAILED`). Yangi `approved-action.ts` + 18 test, `approval.service.spec.ts` + 10 test. `PolicyModule` ↔ `ConnectorsModule` `forwardRef` bilan bog'landi | Founder qarori: streaming yo'lini grafga ko'chirish (467 qator, 2 tarif, token-streaming, halal filtr, compliance packs — pul yo'lining qayta yozilishi) o'rniga **kichik yo'l**. Aniqlandiki to'liq graf holatini tiklash SHART EMAS edi: kerak bo'lgani — tasdiqlangan amalni bajarish. Foydalanuvchi ko'radigan qiymat bir xil, xavf esa tartib darajasida kichik | `apps/api/src/policy/{approval.service,approved-action}.ts`, `src/connectors/connectors.service.ts` (`invokeApproved`), `{policy,connectors}.module.ts`, `blueprints/P0_BLUEPRINT.md` P0-6 §16 |
| 2026-08-17 | **⚠️ Imtiyoz oshirish yuzasi yopildi: tahrirlashda `connector`/`action` QULFLANGAN, faqat `params` o'zgaradi** (`approved-action.ts`) | Tasdiq oynasida foydalanuvchi BITTA amalni ko'radi. Agar tahrirlashda konnektor/amalni almashtirishga ruxsat berilsa, ko'rilgan `google-sheets.read` ni `payme.create_invoice` ga aylantirib yuborish mumkin bo'lardi — ya'ni policy engine bergan tier qarori BOSHQA amalga ko'chirilardi. 5 ta test aynan shuni qulflaydi | `apps/api/src/policy/approved-action.{ts,spec.ts}` |
| 2026-08-17 | **Kill switch tasdiqdan USTUN:** `invokeApproved` amalni bajarishdan oldin `Agent.killedAt` ni tekshiradi | Tasdiq berilgandan keyin agent to'xtatilgan bo'lishi mumkin (foydalanuvchi STOP bosdi yoki admin global kill qildi). "Tasdiq bor edi" bu holatda bajarish uchun asos EMAS — SAFETY_POLICY_LAYER §4 bo'yicha kill switch hamma narsadan ustun | `apps/api/src/connectors/connectors.service.ts` |
| 2026-08-17 | **P0-5 (usage metering) bajarildi — G0.2 marja raqami endi MAVJUD.** Migratsiya `20260817180000_usage_metering`: `UsageEvent` (U1–U10 o'lchamlari, `idempotencyKey @unique`, `internalCostTiyin` BigInt, `costUnknown`) + `UsageKind`. Yangi `src/metering/`: narx jadvali (`[FROM-RESEARCH] 2026-08`), `MeteringService`, `GET /usage/summary` va `GET /admin/economy/margin`. Engine `usage` qaytaradi (Anthropic `final.usage` + OpenRouter `data.usage`), tap uni `UsageEvent` ga yozadi. 18 metering testi; jami 87 suite / 1224 test, 0 regressiya | ADR-023: platforma har chaqiruvda qancha yo'qotayotganini UMUMAN bilmasdi — V3 tartibining asosiy sababi. Shadow rejim: yoziladi, hech kimning balansiga tegmaydi (narx qarori V3-P1, PRICING §8 C3) | `apps/api/prisma/schema.prisma`, `prisma/migrations/20260817180000_*`, `src/metering/*`, `src/events/execution-trace-tap.ts`, `src/agents/agents.service.ts`, `agent-engine/{streaming,openrouter_client}.py` |
| 2026-08-17 | **⚠️ Blueprint baseline'ida xato: `Message.tokensIn/tokensOut` sxemada YO'Q edi.** §1 B7, ADR-023 va METRICS §1 uchalasi "ustunlar mavjud (`schema.prisma:600`), lekin yozilmaydi" degan; aslida ular umuman yo'q va bu ATAYLAB (`schema.prisma:694`: "bo'sh ustun = o'lik sxema"). **Qaror: baribir qo'shilmadi** — `UsageEvent` da `conversationId` bor, ikki joyda saqlash ikkita haqiqat manbai yaratardi | O'sha izohning mantig'i davomi. DoD'ning `Message.tokensIn IS NULL → 0` bandi bekor qilindi va sababi blueprint'da yozildi | `blueprints/P0_BLUEPRINT.md` P0-5 §16, `strategy/METRICS.md` §1 (STALE qator), `adr/ADR-023` |
| 2026-08-17 | **Metering: noma'lum model → `costUnknown: true`, jim 0 EMAS.** Tool-loop aylanishlari QO'SHILADI (almashtirilmaydi) | Jim 0 yozish marjani soxta YAXSHI ko'rsatardi va yangi model qo'shilganda sabab topilmasdi; marja javobida `costUnknownCalls` ochiq sanaladi. Faqat oxirgi aylanish tokenini olish esa real sarfni quyi baholardi (jonli tekshiruv: 2 aylanish → 2468 token, 1234 emas) | `apps/api/src/metering/{model-pricing,metering.service}.ts`, `agent-engine/streaming.py` |
| 2026-08-17 | **`MeteringModule` `UsageModule` dan ALOHIDA.** `usage/` — kvota (kunlik limit, abuse); `metering/` — xarajat o'lchovi | Ikkalasi "usage" so'zini ishlatadi, lekin savollari boshqa: "ruxsat bormi?" va "bizga qancha turdi?". Bitta modulga qo'shish ADR-023 §4 dagi `internal cost` ↔ `user price` ajratilishini xiralashtirardi | `apps/api/src/metering/metering.module.ts` |
| 2026-08-17 | **P0 blueprint TIER A uchun 20 emas, 16 bo'lim ishlatdi.** Tushirilganlar: `User problem`, `Business value`, `UX/UI talablari`, `Cost ta'siri` | Birinchi uchtasi V3 strategiya hujjatlarida (roadmap, pricing, metrics) allaqachon yozilgan — 15 taskda takrorlash SPEC_SYSTEM §10 ogohlantirgan "hujjat fabrikasi"ning aynan o'zi. UX talablari Qism B (TIER B, 11 UI task) ga ko'chdi. `Cost ta'siri` **yo'qolmadi** — u `[BUDGET-BLOCKED]` belgisi va §8 yakuniy jadval ustuni sifatida saqlandi, chunki nol byudjet sharoitida xarajat alohida bo'lim emas, *scope* qarori. Farq §0.2 da ochiq asoslangan edi, lekin jurnalga yozilmagan — endi yozildi | `process/SPEC_SYSTEM.md` §3 (TIER A ta'rifi — o'zgarmaydi), `blueprints/P0_BLUEPRINT.md` §0.2 |
